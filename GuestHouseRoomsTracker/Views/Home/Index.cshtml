@{
    ViewData["Title"] = "Къща за гости ДАРИ";
}
<html>
    <head>
        <link rel="stylesheet" href="~/css/homeIndex.css" />
    </head>
    <body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="floating-element"></div>
        <div class="floating-element"></div>
        <div class="floating-element"></div>

        <div class="hero-content">
            <h1>Къща за гости "Дари"</h1>
            <p>Изживейте комфорт и спокойствие в нашите красиво обзаведени стаи. Вашето перфектно бягство ви очаква.</p>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div>
            <h2 class="section-title fade-in">Защо да изберете нас?</h2>

            <div class="features-grid">
                <div class="feature-card fade-in">
                    <div class="feature-icon">🏠</div>
                    <h3>Комфортни стаи</h3>
                    <p>Просторни, добре обзаведени стаи с модерни удобства и зашеметяваща гледка. Всяка стая е създадена с мисъл за вашия максимален комфорт и отдих.</p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">🍳</div>
                    <h3>Вкусна закуска</h3>
                    <p>Започнете деня си с нашата безплатна закуска, приготвена с местни продукти и свежи, домашни предложения, създадени с любов.</p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">📍</div>
                    <h3>Отлично местоположение</h3>
                    <p>Идеално разположен в близост до местни забележителности, ресторанти и транспорт. Открийте най-доброто от района директно от прага ни.</p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">🌟</div>
                    <h3>Изключително Обслужване</h3>
                    <p>Нашият отдаден персонал е посветен на това да направи престоя ви незабравим. Ние сме тук, за да ви помогнем с препоръки и специални желания.</p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">🚗</div>
                    <h3>Безплатен паркинг</h3>
                    <p>Безплатен и сигурен паркинг, наличен за всички гости. Няма нужда да се притеснявате за място или допълнителни такси за паркиране.</p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">📶</div>
                    <h3>Високоскоростен WiFi</h3>
                    <p>Останете свързани с нашия надежден, високоскоростен интернет в цялата сграда. Перфектен както за почивка, така и за бизнес пътувания.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact" id="contact">
        <div class="container">
            <div class="contact-content fade-in">
                <h2>Готови ли сте да резервирате своя престой?</h2>
                <p style="font-weight:600;">Свържете се с нас още днес, за да резервирате своята стая или да зададете своите въпроси. Ние сме тук, за да направим престоя ви незабравим.</p>

                <div class="contact-info">
                    <div class="contact-item fade-in">
                        <span class="contact-icon">📞</span>
                        <h3>Обадете ни се</h3>
                        <p>+1 (555) 123-4567</p>
                    </div>

                    <div class="contact-item fade-in">
                        <span class="contact-icon">✉️</span>
                        <h3>Изпратете ни email</h3>
                        <p>info@serenityguesthouse.com</p>
                    </div>

                    <div class="contact-item fade-in">
                        <span class="contact-icon">📍</span>
                        <h3>Посетете ни</h3>
                        <p>123 Peaceful Lane<br>Tranquil City, TC 12345</p>
                    </div>
                </div>
            </div>
            <div class="map-container">
                <div class="map-wrapper">
                    <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2926.836481403084!2d27.880237575796894!3d42.81294140637017!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x40a423f63efcb653%3A0xc7cbe181c90726b2!2z0LrRitGJ0LAg0LfQsCDQs9C-0YHRgtC4ICLQlNCQ0KDQmCIt0J7QsdC30L7RgA!5e0!3m2!1sbg!2sbg!4v1753536363319!5m2!1sbg!2sbg"
                            allowfullscreen=""
                            loading="lazy"
                            referrerpolicy="no-referrer-when-downgrade"
                            title="Guest House Location Map">
                    </iframe>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Scroll Animation
        function revealElements() {
            const elements = document.querySelectorAll('.fade-in');
            const windowHeight = window.innerHeight;

            elements.forEach(element => {
                const elementTop = element.getBoundingClientRect().top;
                const elementVisible = 150;

                if (elementTop < windowHeight - elementVisible) {
                    element.classList.add('visible');
                }
            });
        }

        window.addEventListener('scroll', revealElements);
        window.addEventListener('load', revealElements);

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Admin Login Function
        function showAdminLogin() {
            const password = prompt('Enter admin password:');
            if (password === 'admin123') {
                alert('Login successful! Redirecting to admin panel...');
                // Here you would redirect to your admin panel
                // window.location.href = '/admin';
            } else if (password !== null) {
                alert('Invalid password. Access denied.');
            }
        }

        // Add parallax effect to hero section
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const hero = document.querySelector('.hero');
            if (hero) {
                hero.style.transform = `translateY(${scrolled * 0.5}px)`;
            }
        });

        // Add interactive hover effects
        document.querySelectorAll('.feature-card').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-10px) scale(1.02)';
            });

            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });
    </script>
    </body>
</html>

